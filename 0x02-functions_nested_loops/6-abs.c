#include "main.h"
#include <stdio.h>

/**
 * _abs - computes the absolute value of an integer
 * @n: parameter
 * Return: absolute value of n integer
 */

int _abs(int ab)
{
	if (n < 0)
		n = -n;
	return (n);
}
