#include "main.h"
#include <stdio.h>

/**
 * _abs - computes the absolute value of an integer
 * @n: The input letter
 * Return: Always success
 */

int _abs(int ab)
{
	return (ab * ((ab > 0) - (ab <0)));
}
